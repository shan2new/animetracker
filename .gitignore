node_modules
dist
*.local
.DS_Store
.env
.env.*
!.env.example
*.log

# Plain-SQL data dump — contains user emails + Clerk IDs, must never be committed
anitrack-2026-06-24.sql

# Local fastembed ONNX model cache (re-downloadable, ~87MB)
.fastembed_cache/
